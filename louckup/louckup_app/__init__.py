"""Louckup — eigenständiger, isolierter Bereich unter <url>/louckup.

Bewusst *kein* Import aus `phantom`, `bot` oder `dashboard`. Alles, was
Louckup braucht, liegt in diesem Ordner:
eigene Config, eigene OAuth-App, eigene Datenbank, eigene Cookies.
"""
