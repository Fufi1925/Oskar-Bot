# Louckup — eigene Abhängigkeiten.
#
# Der Bereich läuft im selben Python-Interpreter wie der Bot (er wird in
# die FastAPI-App gemountet). Diese Liste ist trotzdem getrennt, damit
# Louckup auch allein stehen kann und damit klar bleibt, was es braucht.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.4
itsdangerous>=2.2.0
httpx>=0.27.0
aiosqlite>=0.20.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
